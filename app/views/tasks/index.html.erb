<h1>Here are your tasks!</h1>
<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title , show_path(task)%> <%=task.completed %></li>
    <% end %>
</ul>

<%= link_to "Add new task", tasks_new_path %>
